name: localization

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:

  flutter:
    sdk: flutter
  localized_string: 0.2.0
  intl: 0.18.0
  intl_utils: 2.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_intl:
  enabled: true # Required. Must be set to true to activate the package. Default: false
  class_name: AppString # Optional. Sets the name for the generated localization class. Default: S
  main_locale: ru # Optional. Sets the main locale used for generating localization files. Provided value should consist of language code and optional script and country codes separated with underscore (e.g. 'en', 'en_GB', 'zh_Hans', 'zh_Hans_CN'). Default: en
  arb_dir: lib/l10n # Optional. Sets the directory of your ARB resource files. Provided value should be a valid path on your system. Default: lib/l10n
  output_dir: lib/generated
